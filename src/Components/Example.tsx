import { Container, Title } from '@mantine/core';
import { FC } from 'react';

export const Example: FC = () => {
  return (
    <Container>
      <Title pt={20}>Example</Title>
    </Container>
  );
};
